using System.Collections;
using System.Collections.Generic;
using UnityEngine;

enum tipo { Monstera, Peyote};
public class Planta : MonoBehaviour
{
    tipo tipo_;
    

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
